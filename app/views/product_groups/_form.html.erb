<%= form_with(model: product_group, local: true) do |f| %>
  <% if product_group.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(product_group.errors.count, "error") %> prohibited this product_group from being saved:</h3>

      <ul>
      <% product_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_category_id, "Product Category: " %>
    <%= f.collection_select :product_category_id, active_product_categories, :id, :name, prompt: true %>
  </div>


  <div class="field">
    <%= f.label :name, "Product Group Name: " %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :active, "Active?" %>
    <%= f.hidden_field :active, value: 0 %>
    <%= f.check_box :active %><br/>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

